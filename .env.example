# Server-side only. Never expose this as VITE_OPENAI_API_KEY.
OPENAI_API_KEY=

# Optional. Use a current OpenAI model that supports text, image input, and structured JSON output.
# If omitted, /api/analyze-admin uses a conservative default in the serverless route.
OPENAI_MODEL=
